import requests
import typing
import re
import logging
import numpy as np
import sklearn
import os

# Псевдокод / Pseudocode


class AI:
    def __init__(self, api_url: str):
        
    def summarize_text(self, text: str) -> str:
        
    def summarize_categories(self, text: str, categories: list) -> str:

    def get_embedding(self, text: str) -> list:
